; Raise PHP upload limits for property media in admin (XAMPP / mod_php).
; Restart Apache after changing if .user.ini is not picked up immediately.
upload_max_filesize = 512M
post_max_size = 768M
max_execution_time = 600
max_input_time = 600
